import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    button: {
        marginLeft: 'auto',
        marginRight: 'auto'
    },
    shadow: {
        position: 'absolute',
        marginLeft: '2%'
    }
});

export default styles;